# 快速测试指南

## ✅ 问题已修复

我已经修复了 ASCII 编码错误，并做了以下改进：

1. ✅ **修复编码问题** - 现在可以正常发送中文消息
2. ✅ **修复配置文件读取** - 程序自动从 `config.yaml` 读取所有设置
3. ✅ **修正模型名称** - `config.yaml` 中的模型名已改为标准格式
4. ✅ **改进错误处理** - 更友好的错误提示

---

## 🚀 现在可以运行了

### 方法 1：命令行运行
```bash
cd /Users/wuxinyang/Desktop/sakura-main/simple-ai-agent
python3 main.py
```

### 方法 2：直接双击
在 Finder 中双击 `main.py`（如果已设置 Python 关联）

---

## 👀 启动时应该看到

终端会显示：
```
✅ 成功加载配置：config.yaml
```

然后弹出窗口，就可以开始聊天了！

---

## 🧪 测试步骤

### 1. 测试中文输入
- 在输入框输入：`你好，今天天气怎么样？`
- 点击"发送"或按回车
- 应该能看到 AI 的正常回复（不会再报编码错误）

### 2. 测试配置读取
- 关闭程序
- 打开 `config.yaml`，修改系统提示词：
  ```yaml
  system_prompt: "你是一个爱开玩笑的 AI 助手。"
  ```
- 重新运行程序，AI 的回复风格会改变

### 3. 测试历史记录
- 发送几条消息
- 关闭程序
- 重新打开，历史对话应该还在

---

## ❓ 如果还有问题

### 问题 1：提示"API 调用失败"
**原因**：可能是 API Key 或网络问题

**解决**：
1. 检查 `config.yaml` 中的 `api_key` 是否正确
2. 检查 `base_url` 是否能访问（可以在浏览器打开）
3. 查看具体错误信息（会显示在界面上）

### 问题 2：窗口一闪就关闭
**原因**：可能是 Python 环境问题

**解决**：
```bash
# 在终端运行，查看具体错误
cd /Users/wuxinyang/Desktop/sakura-main/simple-ai-agent
python3 main.py
```

### 问题 3：提示"配置文件不存在"
**原因**：运行目录不对

**解决**：
```bash
# 确保在项目根目录运行
cd /Users/wuxinyang/Desktop/sakura-main/simple-ai-agent
ls config.yaml  # 应该能看到文件
python3 main.py
```

---

## 📊 修复内容总结

| 修复项 | 状态 |
|--------|------|
| ASCII 编码错误 | ✅ 已修复 |
| 配置文件读取 | ✅ 已实现 |
| 模型名称格式 | ✅ 已修正 |
| 错误提示改进 | ✅ 已完成 |
| 依赖更新 | ✅ 已添加 PyYAML |

---

## 📄 相关文档

- **开发日志**：`logs/stage1_mvp_log.md`（阶段 1 完整说明）
- **修复日志**：`logs/fix_ascii_encoding_error.md`（本次修复详情）
- **使用指南**：`README.md`（完整使用说明）

---

**现在就去试试吧！有问题随时告诉我 😊**
