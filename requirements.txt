# 简易 AI 桌面助手 - 依赖包列表
# 安装方法：pip install -r requirements.txt

# Qt 图形界面库（用于创建窗口和控件）
PySide6>=6.5.0

# OpenAI Python 库（用于调用 AI API）
openai>=1.0.0

# YAML 配置文件解析库
PyYAML>=6.0

# DuckDuckGo 搜索库（用于联网搜索）
ddgs>=9.0.0

# RAG 知识库（向量数据库 + 文本嵌入）
chromadb>=1.5.0
sentence-transformers>=5.0.0
